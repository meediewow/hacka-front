import { styled } from '@mui/material/styles';

export const CloseIconButtonSC = styled('span')`
    position: absolute;
    top: ${(props) => props.theme.spacing(0.5)};
    right: ${(props) => props.theme.spacing(0.5)};
`;
